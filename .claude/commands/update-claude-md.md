Given the recent progress (check MEMORY and changelog), update CLAUDE.md. 
This file will be send to *every* coding agent regardless of the type of tasks, so you should only include absolute necessary information that set the high-level context. You should not include anything that can be inferred by reading the code (e.g. coding standard)
This file cannot exceed 100 lines.